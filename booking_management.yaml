openapi: 3.0.0
info:
  title: Paziresh24 booking management
  version: '1.0'
  description: |
    Management system for doctors or facilities.

    <div dir="rtl" lang="fa">
    <strong>راهنمای تنظیم توکن</strong><br/>
    ۱) ابتدا با فراخوانی <code>/v1/login</code> مقدار <code>certificate</code> را دریافت کنید.<br/>
    ۲) سپس در بالای صفحه روی دکمه <code>Authorize</code> یا آیکون قفل 🔒 کلیک کنید.<br/>
    ۳) مقدار دریافت‌شده را در فیلد <code>X-API-KEY</code> وارد کرده و روی <code>Authorize</code> بزنید.<br/>
    پس از آن می‌توانید روی <code>Try it out</code> کلیک کرده و درخواست‌ها را ارسال کنید.
    </div>
  termsOfService: https://www.paziresh24.com/home/rules/
  contact:
    email: info@paziresh24.com
    name: paziresh24
    url: https://www.paziresh24.com
servers:
  - url: https://apigw.paziresh24.com/booking-management
    variables: {}
paths:
  /v1/login:
    summary: Get key to use management booking apis
    post:
      summary: Get key to use management apis
      description: ''
      operationId: ''
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                username:
                  type: string
                password:
                  type: string
                center_id:
                  type: string
              required:
                - username
                - password
                - center_id
      responses:
        '200':
          description: when api key generated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  result:
                    type: object
                    properties:
                      certificate:
                        type: string
      tags:
        - Auth
  /v1/appointments:
    get:
      summary: get list of appointments
      description: "<div dir=\"rtl\" lang=\"fa\">پارامتر center_id الزامی است و سایر پارامترها اختیاری هستند. در صورتی که نوبت‌های یک پزشک خاص را می‌خواهید، مقدار user_center_id یا medical_code را وارد نمایید.<br/><br/><strong>نکته:</strong> برای تنظیم توکن، روی آیکون قفل 🔒 یا دکمه Authorize کلیک کنید و مقدار X-API-KEY را وارد نمایید.</div>"
      operationId: ''
      security:
        - ApiKey: []
      parameters:
        - name: date_start
          in: query
          description: "<span dir=\"rtl\" lang=\"fa\">فرمت تاریخ ISO 8601. می‌توانید بدون ساعت هم ارسال کنید (مثال: 2023-09-19).</span>"
          examples:
            dateTime:
              summary: 'تاریخ و ساعت'
              value: '2023-09-19T00:00:00Z'
            dateOnly:
              summary: 'فقط تاریخ'
              value: '2023-09-19'
          schema:
            oneOf:
              - type: string
                format: date-time
              - type: string
                format: date
        - name: date_end
          in: query
          description: "<span dir=\"rtl\" lang=\"fa\">فرمت تاریخ ISO 8601. می‌توانید بدون ساعت هم ارسال کنید (مثال: 2023-09-19).</span>"
          examples:
            dateTime:
              summary: 'تاریخ و ساعت'
              value: '2023-09-19T23:59:59Z'
            dateOnly:
              summary: 'فقط تاریخ'
              value: '2023-09-19'
          schema:
            oneOf:
              - type: string
                format: date-time
              - type: string
                format: date
        - name: medical_code
          in: query
          schema:
            type: string
        - name: user_center_id
          in: query
          schema:
            type: string
            format: uuid
        - name: center_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: ref_id
          in: query
          required: false
          schema:
            type: string
          description: |
            <div dir="rtl" lang="fa">
            شناسه پیگیری/رفرنس نوبت (ref_id)
            توجه: این پارامتر ممکن است برای چند نوبت مختلف یکسان باشد. برای اطمینان از بازیابی صحیح نوبت خود، پیشنهاد می‌شود علاوه‌بر ref_id، کد نظام پزشکی doctor (پارامتر medical_code) را نیز وارد کنید.
            </div>
      responses:
        '200':
          description: List of books
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/appointment'
      tags:
        - Appointment
components:
  schemas:
    appointment:
      description: an appointment info
      properties:
        id:
          type: string
          format: uuid
        from:
          type: integer
          example: 1695094200
        to:
          type: integer
          example: 1695094200
        from_iso:
          type: string
          format: date-time
        to_iso:
          type: string
          format: date-time
        book_register:
          type: integer
          example: 1695094200
        reference_code:
          type: string
          example: 1695022717484
        book_status:
          type: integer
          example: 1
        paziresh_status:
          type: integer
          example: 1
        payment_status:
          type: integer
          example: 1
        book_type:
          type: integer
          description: "<div dir=\"rtl\" lang=\"fa\">نوع نوبت: وب سایت مرکز=1، تلفنی=2، حضوری=3، اپلیکیشن مرکز=4، تلگرام=5، کیوسک فراخوان=6، وب سایت پذیرش24=7، اپلیکیشن پذیرش24=8، درخواست نوبت=11، نظام ارجاع=12، USSD=13</div>"
          enum: [1, 2, 3, 4, 5, 6, 7, 8, 11, 12, 13]
          example: 12
        patient:
          $ref: '#/components/schemas/patient'
        doctor:
          $ref: '#/components/schemas/doctor'
        center:
          $ref: '#/components/schemas/center'
        payment:
          $ref: '#/components/schemas/payment'
          nullable: true
          description: "<div dir=\"rtl\" lang=\"fa\">فقط برای مراکزی که پرداخت فعال دارند برگردانده می‌شود. در صورت عدم وجود، این فیلد null است.</div>"
        referral_tqid:
          type: string
          nullable: true
          description: "<div dir=\"rtl\" lang=\"fa\">شناسه پیگیری نوبت در نظام ارجاع (Tracking Queue ID). فقط برای نوبت‌های نظام ارجاع وجود دارد.</div>"
          example: "4:cb67dc00-a47b-4ff8-b51b-4621051c47d1"
        referral_status:
          type: integer
          nullable: true
          description: "<div dir=\"rtl\" lang=\"fa\">وضعیت نوبت در نظام ارجاع: 1=خالی، 2=اخذ شده، 3=ویزیت شده، 4=لغو شده توسط بیمار، 5=لغو شده توسط پزشک، 6=لغو شده به علت تاخیر بیمار</div>"
          enum: [1, 2, 3, 4, 5, 6]
          example: 2
        book_number:
          type: string
          nullable: true
          description: "<div dir=\"rtl\" lang=\"fa\">شماره نوبت. در صورت عدم وجود، خالی است.</div>"
          example: ""

    patient:
      description: patient info
      properties:
        name:
          type: string
          example: علی
        family:
          type: string
          example: صدر
        cell:
          type: string
          nullable: true
          description: "<div dir=\"rtl\" lang=\"fa\">شماره موبایل بیمار. در صورت عدم وجود می‌تواند خالی باشد.</div>"
          example: 913456789
        national_code:
          type: string
          example: 1234567890
        gender:
          type: integer
          nullable: true
          description: "<div dir=\"rtl\" lang=\"fa\">جنسیت: 1=مرد، 2=زن</div>"
          enum: [1, 2]
          example: 1
    doctor:
      description: doctor info
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: علیرضا
        family:
          type: string
          example: صدر
        medical_code:
          type: string
          example: 153815
        user_center_id:
          type: string
          format: uuid
    center:
      description: center info
      properties:
        id:
          type: string
          description: "<div dir=\"rtl\" lang=\"fa\">شناسه مرکز که می‌تواند عددی (مراکز قدیمی) یا UUID (مراکز جدید) باشد</div>"
          example: "379"
        name:
          type: string
        address:
          type: string
          nullable: true
          description: "<div dir=\"rtl\" lang=\"fa\">آدرس مرکز. در صورت وجود برگردانده می‌شود.</div>"
    payment:
      description: payment info
      properties:
        book_status:
          type: string
          enum: ['paid','not-paid', 'paying', 'refunded', 'refund-request']
        factor_status:
          type: string
          nullable: true
          description: "<div dir=\"rtl\" lang=\"fa\">وضعیت فاکتور. می‌تواند خالی باشد.</div>"
          enum: ['paid', 'not-paid', 'expired', 'paying', 'refunded', '']
        cost:
          type: integer
  securitySchemes:
    ApiKey:
      type: apiKey
      in: header
      name: X-API-KEY
      description: "<div dir=\"rtl\" lang=\"fa\">برای احراز هویت، مقدار <code>certificate</code> را از سرویس <code>/v1/login</code> دریافت کرده و در فیلد <code>X-API-KEY</code> وارد کنید. در Swagger UI با کلیک روی دکمه <code>Authorize</code> یا آیکون قفل 🔒 می‌توانید این مقدار را تنظیم کنید.</div>"
